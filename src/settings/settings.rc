/* Settings' resources: its icon (drawn at build time by gen-icon.py) and a
 * common-controls 6 manifest (themed buttons, sliders, the UAC shield). */
#include <winuser.h>
1 ICON "sg-settings.ico"
CREATEPROCESS_MANIFEST_RESOURCE_ID RT_MANIFEST "settings.manifest"
